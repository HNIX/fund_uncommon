<div class="container mx-auto my-8 px-4">
  <div class="max-w-3xl mx-auto">
    <div class="flex justify-between items-center mb-4">
      <h1 class="h3"><%= link_to "Leads", leads_path %> > Lead #<%= @lead.id %></h1>
      <%= link_to t("edit"), edit_lead_path(@lead), class: "btn btn-link" %>
    </div>

    <div class="bg-white rounded shadow p-8">
            <div class="mb-4">
        <h5>First name</h5>
        <%= @lead.first_name %>
      </div>
            <div class="mb-4">
        <h5>Last name</h5>
        <%= @lead.last_name %>
      </div>
            <div class="mb-4">
        <h5>Email</h5>
        <%= @lead.email %>
      </div>
            <div class="mb-4">
        <h5>Phone</h5>
        <%= @lead.phone %>
      </div>
            <div class="mb-4">
        <h5>Address</h5>
        <%= @lead.address %>
      </div>
            <div class="mb-4">
        <h5>Address2</h5>
        <%= @lead.address2 %>
      </div>
            <div class="mb-4">
        <h5>City</h5>
        <%= @lead.city %>
      </div>
            <div class="mb-4">
        <h5>Address state</h5>
        <%= @lead.address_state %>
      </div>
            <div class="mb-4">
        <h5>Zip code</h5>
        <%= @lead.zip_code %>
      </div>
            <div class="mb-4">
        <h5>User</h5>
        <%= @lead.user %>
      </div>
            <div class="mb-4">
        <h5>Source</h5>
        <%= @lead.source %>
      </div>
            <div class="mb-4">
        <h5>Account</h5>
        <%= @lead.account %>
      </div>
            <div class="mb-4">
        <h5>Marketing accepted</h5>
        <%= @lead.marketing_accepted %>
      </div>
          </div>
  </div>
</div>
