<!-- This example requires Tailwind CSS v2.0+ -->
    <div class="py-4 text-left sm:mb-8 sm:align-middle w-full">
        <div>
        
        <div class="mt-3 text-left sm:mt-5">
            <h3 class="text-lg leading-6 font-medium text-gray-900" id="modal-title">
            Send leads from any app
            </h3>
            <div class="mt-2">
            <p class="text-sm text-gray-500">
                In case you need the simplest integration we could think of, you can always send us HTTP POST requests with leads as JSON objects. 
            </p>
            </div>
            <div class="mt-4">
            <p class="text-sm text-gray-500 font-semibold">
                Send the POST request to: 
            </p>
            
            </div>
            <div class="mt-2 py-2 px-4 bg-gray-200 rounded text-sm text-gray-500 break-words text-left">
                sources.lvh.me:5000/rest/<%= @source.webhook_token %>
            </div>
            <div class="mt-4">
            <p class="text-sm text-gray-500 font-semibold">
                Make sure to include the following header:  
            </p>
            
            </div>
            <div class="mt-2 py-2 px-4 bg-gray-200 rounded text-sm text-gray-500 break-words text-left">
                Content-Type: application/json
            </div>
        </div>
        </div>
        <div class="mt-5 sm:mt-6">
        <button type="button" class="w-full inline-flex justify-center rounded-md border border-transparent shadow-sm px-4 py-2 bg-indigo-600 text-base font-medium text-white hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 sm:col-start-2 sm:text-sm">
            Share Webhook
        </button>
        </div>
    </div>
